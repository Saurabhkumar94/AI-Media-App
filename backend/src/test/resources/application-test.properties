# --- Testing ke liye H2 Database Configuration ---
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;NOT_ONLY_READ_COMMITTED=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=root
spring.datasource.password=Sau@123
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# --- Mock Keys (Taki API call fail na ho) ---
google.gemini.key=mock-key
deepgram.api.key=mock-key

# --- Hibernate Logging (Optional: Debugging ke liye) ---
spring.jpa.show-sql=true